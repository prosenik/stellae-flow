{
  "name": "stellae-flow",
  "version": "1.0.0",
  "description": "Auto-generate user flow diagrams from Figma prototype connections",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "dagre": "^0.8.5",
    "@types/dagre": "^0.7.52",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
